name: Daily Scraper

on:
  schedule:
    - cron: '0 0 * * *'   # Runs every day at midnight UTC
  workflow_dispatch:       # Allows manual run

jobs:
  scraper:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_API_KEY: ${{ secrets.SUPABASE_API_KEY }}
          FOOTBALL_DATA_TOKEN: ${{ secrets.FOOTBALL_DATA_TOKEN }}
        run: |
          echo "[workflow] SUPABASE_URL present: ${SUPABASE_URL:+yes}${SUPABASE_URL:+' (masked)'}"
          echo "[workflow] SUPABASE_API_KEY present: ${SUPABASE_API_KEY:+yes}${SUPABASE_API_KEY:+' (masked)'}"
          echo "[workflow] FOOTBALL_DATA_TOKEN present: ${FOOTBALL_DATA_TOKEN:+yes}${FOOTBALL_DATA_TOKEN:+' (masked)'}"
          python scraper.py
